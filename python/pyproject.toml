[build-system]
requires = ["maturin>=1.12,<2.0"]
build-backend = "maturin"

[project]
name = "datafusion-raquet-python"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["datafusion>=54"]

[tool.maturin]
python-source = "python"
module-name = "datafusion_raquet._internal"
